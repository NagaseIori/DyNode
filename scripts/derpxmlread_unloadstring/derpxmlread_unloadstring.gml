/// @description  DerpXmlRead_UnloadString()
function DerpXmlRead_UnloadString() {
	//
	//  Empties the input xml string (if reading from string) so its memory can be freed later.

	objDerpXmlRead.xmlString = ""



}
